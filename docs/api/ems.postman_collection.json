{
  "info": {
    "name": "EMS (IDMS)",
    "description": "Collection API untuk modul EMS (CRUD + file upload). Setelah login, token JWT otomatis disimpan ke variabel `token` (via Test script pada request Login), lalu dipakai untuk semua endpoint lainnya. Variabel `ems_id` otomatis terisi dari respon Store untuk dipakai pada request Show/Update/Delete/Download.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "variable": [
    { "key": "baseUrl", "value": "http://127.0.0.1:8100", "type": "string" },
    { "key": "token", "value": "", "type": "string" },
    { "key": "ems_id", "value": "", "type": "string" }
  ],
  "auth": {
    "type": "bearer",
    "bearer": [{ "key": "token", "value": "{{token}}", "type": "string" }]
  },
  "item": [
    {
      "name": "1. Autentikasi",
      "item": [
        {
          "name": "Login (ambil token)",
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"username\": \"admin\",\n  \"password\": \"admin1\"\n}"
            },
            "url": { "raw": "{{baseUrl}}/api/login", "host": ["{{baseUrl}}"], "path": ["api", "login"] },
            "auth": { "type": "noauth" }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const json = pm.response.json();",
                  "if (json.success && json.data && json.data.token) {",
                  "  pm.collectionVariables.set('token', json.data.token);",
                  "  pm.environment.set('token', json.data.token);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "Logout",
          "request": {
            "method": "POST",
            "url": { "raw": "{{baseUrl}}/api/logout", "host": ["{{baseUrl}}"], "path": ["api", "logout"] }
          }
        }
      ]
    },
    {
      "name": "2. EMS",
      "item": [
        {
          "name": "Index (semua data)",
          "request": {
            "method": "GET",
            "url": { "raw": "{{baseUrl}}/api/ems", "host": ["{{baseUrl}}"], "path": ["api", "ems"] }
          }
        },
        {
          "name": "Index + filter tag_number_id",
          "request": {
            "method": "GET",
            "url": {
              "raw": "{{baseUrl}}/api/ems?tag_number_id=7",
              "host": ["{{baseUrl}}"],
              "path": ["api", "ems"],
              "query": [
                { "key": "tag_number_id", "value": "7", "description": "filter berdasarkan tag number id (opsional)" }
              ]
            }
          }
        },
        {
          "name": "Index + search & range tanggal",
          "request": {
            "method": "GET",
            "url": {
              "raw": "{{baseUrl}}/api/ems?search=2026&tanggal_start=2026-01-01&tanggal_end=2026-12-31",
              "host": ["{{baseUrl}}"],
              "path": ["api", "ems"],
              "query": [
                { "key": "search", "value": "2026", "description": "cari berdasarkan no_ems atau tag_number (opsional)" },
                { "key": "tanggal_start", "value": "2026-01-01", "description": "filter tanggal mulai (opsional)" },
                { "key": "tanggal_end", "value": "2026-12-31", "description": "filter tanggal akhir (opsional)" }
              ]
            }
          }
        },
        {
          "name": "Index + pagination",
          "request": {
            "method": "GET",
            "url": {
              "raw": "{{baseUrl}}/api/ems?per_page=5",
              "host": ["{{baseUrl}}"],
              "path": ["api", "ems"],
              "query": [
                { "key": "per_page", "value": "5", "description": "aktifkan pagination (kosongkan untuk semua data)" }
              ]
            }
          }
        },
        {
          "name": "Show",
          "request": {
            "method": "GET",
            "url": {
              "raw": "{{baseUrl}}/api/ems/{{ems_id}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "ems", "{{ems_id}}"]
            }
          }
        },
        {
          "name": "Store (tambah)",
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "multipart/form-data" }],
            "body": {
              "mode": "formdata",
              "formdata": [
                { "key": "tag_number_id", "value": "7", "type": "text", "description": "id tag number (required)" },
                { "key": "no_ems", "value": "EMS-2026-001", "type": "text", "description": "nomor EMS (required)" },
                { "key": "tanggal", "value": "2026-09-01", "type": "text", "description": "tanggal (required)" },
                { "key": "file", "value": "", "type": "file", "description": "file EMS, semua tipe (opsional)" }
              ]
            },
            "url": { "raw": "{{baseUrl}}/api/ems", "host": ["{{baseUrl}}"], "path": ["api", "ems"] }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const json = pm.response.json();",
                  "if (json.success && json.data && json.data.id) {",
                  "  pm.collectionVariables.set('ems_id', String(json.data.id));",
                  "  pm.environment.set('ems_id', String(json.data.id));",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "Update",
          "request": {
            "method": "PUT",
            "header": [{ "key": "Content-Type", "value": "multipart/form-data" }],
            "body": {
              "mode": "formdata",
              "formdata": [
                { "key": "tag_number_id", "value": "7", "type": "text", "description": "id tag number (opsional)" },
                { "key": "no_ems", "value": "EMS-2026-001-updated", "type": "text", "description": "nomor EMS (opsional)" },
                { "key": "tanggal", "value": "2026-09-02", "type": "text", "description": "tanggal (opsional)" },
                { "key": "file", "value": "", "type": "file", "description": "file EMS baru, ganti file lama bila ada (opsional)" }
              ]
            },
            "url": {
              "raw": "{{baseUrl}}/api/ems/{{ems_id}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "ems", "{{ems_id}}"]
            }
          }
        },
        {
          "name": "Delete",
          "request": {
            "method": "DELETE",
            "url": {
              "raw": "{{baseUrl}}/api/ems/{{ems_id}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "ems", "{{ems_id}}"]
            }
          }
        },
        {
          "name": "Download file",
          "request": {
            "method": "GET",
            "url": {
              "raw": "{{baseUrl}}/api/ems/download_file/{{ems_id}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "ems", "download_file", "{{ems_id}}"]
            }
          }
        }
      ]
    }
  ]
}
